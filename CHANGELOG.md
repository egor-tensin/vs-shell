v2
--

* `Win32` is an alias for `x86` as the `platform` parameter value.
* Support GitHub-hosted ARM runners. The `platform` parameter supports value
  `arm64` (and an alias `arm`).

v1
--

* Initial release.
